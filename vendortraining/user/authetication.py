from vendortraining.models import User
from django.contrib.auth.models import User


class UserAuthetication(viewsets.ModelViewSet):

    def checkUser(user_id)
        if user_id = self.request.data.get('user_id'):
            return True
        elif user_id:
            return False    

    def checkId(user_id)
        if 
